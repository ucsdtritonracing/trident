[project]
name = "trident-generators"
version = "0.1.0"
description = "Code generators for the trident monorepo"
requires-python = ">=3.11"
dependencies = [
    # add as needed, e.g.:
    # "python-can>=4.3",
    # "click>=8.1",
]